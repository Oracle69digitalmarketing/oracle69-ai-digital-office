# API Client Configuration

To connect the frontend to the live backend, add the following environment variable to your frontend `.env.local` file:

```env
NEXT_PUBLIC_API_URL=https://oracle69-ai-digital-office.onrender.com/api
```
